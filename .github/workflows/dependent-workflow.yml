name: Dependent Workflow

on:
  workflow_run:
    workflows: ["Main Workflow"]
    types:
      - completed

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Notify
        run: echo "The main workflow has completed!"
